*~
# Everything the build makes is in one of two directories: the objects, the
# generated sources, the tests, the demos and everything a test run leaves
# behind land under build/, and the programs that come out land in bin/. See
# the top level Makefile.
build/
bin/
